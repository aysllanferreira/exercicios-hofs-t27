/* eslint-disable no-console */
const array = [3, 5, 1, 6, 2, 4, 7, 9, 8, 10];

// Some todos os numeros do array usando forEach

const foreachFunc = () => {
  // Seu codigo aqui...
};

// Some todos os numeros do array usando reduce
const reduce = () => {
  // Seu codigo aqui...
};

module.exports = { foreachFunc, reduce };
